# Required: password for the dashboard
ADMIN_PASSWORD=change-me

# Recommended: keeps dashboard logins valid across server restarts.
# Generate with: openssl rand -hex 32
SECRET=

# Optional
PORT=3000
DATA_DIR=./data
